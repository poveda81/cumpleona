# API Keys para generación de imágenes
# Necesitas al menos una de estas claves dependiendo del proveedor que uses

# Para usar Google Imagen via Vertex AI (--provider google)
# Necesitas configurar un proyecto de Google Cloud con facturación activa
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Para usar OpenAI DALL-E 3 (--provider openai)
OPENAI_API_KEY=